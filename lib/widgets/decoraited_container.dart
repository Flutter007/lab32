import 'package:flutter/material.dart';

class DecoraitedContainer extends StatelessWidget {
  const DecoraitedContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
